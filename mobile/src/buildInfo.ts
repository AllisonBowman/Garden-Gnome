// Overwritten by scripts/sim-build.sh at build time so the running app can show
// exactly which commit/build it is (Settings → About). These committed defaults
// apply to EAS/production builds where the script doesn't run.
export const BUILD_COMMIT = 'production';
export const BUILD_NUMBER = 0;
export const BUILT_AT = '';
